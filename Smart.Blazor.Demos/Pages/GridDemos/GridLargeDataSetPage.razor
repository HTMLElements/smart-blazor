@page "/grid-large-data-set"

@using Smart.Blazor.Demos.Data
@inject RandomDataService dataService

<style>
    body,
    html,
    app {
        height: 100%;
    }

    app {
        overflow: auto;
    }

    .content {
        height: calc(100% - 70px);
    }
    /* This is the CSS used in the demo */
    smart-grid {
        width: 60%;
        max-width: 800px;
    }
</style>
<Example Name="Grid">
    <p>The Grid in this example is with 1000 columns, 10,000 rows</p>

    <Grid DataSourceSettings="dataSourceSettings" DataSource="100" Appearance="@appearance" OnCellValue="OnCellValue"></Grid>    
</Example>

@code {
    GridDataSourceSettings dataSourceSettings = new GridDataSourceSettings()
    {
        //DataFields = 1000
    };

    GridAppearance appearance = new GridAppearance() { ShowRowHeaderNumber = true };

    private void OnCellValue(object cell)
    {
        Console.WriteLine(cell);
    }
}
