@namespace Smart.Blazor
<div>

  <h2 id="MultilineTextBoxProperties"><a id="MultilineTextBox_properties" href="javascript: void(0)" class="anchor"><strong>MultilineTextBox</strong> Properties</a></h2>
  <table class="table">
      <tr><th>Name</th><th>Type</th><th>Default</th><th>Description</th></tr>
      <tr><td>Animation</td><td>Animation</td><td>Animation.Advanced</td><td>Sets or gets the animation mode. Animation is disabled when the property is set to <strong>'none'</strong></td></tr>
      <tr><td>AutoCapitalize</td><td>MultilineTextBoxAutoCapitalize</td><td>MultilineTextBoxAutoCapitalize.None</td><td>Determines whether and how the value should be automatically capitalized as it is entered/edited by the user.</td></tr>
      <tr><td>AutoComplete</td><td>MultiLineTextBoxAutoComplete</td><td>MultiLineTextBoxAutoComplete.Off</td><td>Determines whether the value of the control can be automatically completed by the browser.</td></tr>
      <tr><td>AutoExpand</td><td>bool</td><td>false</td><td>Determines whether element will auto expand when the input overflows vertically.</td></tr>
      <tr><td>AutoFocus</td><td>bool</td><td>false</td><td>Determines whether the input should be focused when the page is loaded.</td></tr>
      <tr><td>Cols</td><td>int</td><td>20</td><td>The cols attribute specifies the visible width of a input. If it is specified, it must be a positive integer. If it is not specified, the default value is 20.</td></tr>
      <tr><td>Disabled</td><td>bool</td><td>false</td><td>Enables or disables the element.</td></tr>
      <tr><td>DisplayMode</td><td>TextBoxDisplayMode</td><td>TextBoxDisplayMode.Default</td><td>Specifies how the characters are displayed inside the input.</td></tr>
      <tr><td>EnterKeyBehavior</td><td>MultilineTextBoxEnterKeyBehavior</td><td>MultilineTextBoxEnterKeyBehavior.NewLine</td><td>Determines the behavior on "Enter" key.</td></tr>
      <tr><td>Form</td><td>string</td><td>&quot;&quot;</td><td>The form element that the element is associated with (its "form owner"). The value of the attribute must be the ID of a form element in the same document.</td></tr>
      <tr><td>Hint</td><td>object</td><td>N/A</td><td>Sets additional helper text below the element. Appears only when the element is focused.</td></tr>
      <tr><td>HorizontalScrollBarVisibility</td><td>HorizontalScrollBarVisibility</td><td>HorizontalScrollBarVisibility.Auto</td><td>Controls horizontal scrollbar's visibility. </td></tr>
      <tr><td>InputPurpose</td><td>string</td><td>&quot;off&quot;</td><td>Sets the purpose of the input and what, if any, permission the user agent has to provide automated assistance in filling out the element's input when in a form, as well as guidance to the browser as to the type of information expected in the element. This value corresponds to the standard HTML <strong>autocomplete</strong> attribute and can be set to values such as <em>'on'</em>, <em>'name'</em>, <em>'organization'</em>, <em>'street-address'</em>, etc.</td></tr>
      <tr><td>Label</td><td>string</td><td>&quot;&quot;</td><td>Sets label above the element. The label is displayed above the input and it's always visible.</td></tr>
      <tr><td>Locale</td><td>string</td><td>&quot;en&quot;</td><td>Sets or gets the language. Used in conjunction with the property <strong>messages</strong>. </td></tr>
      <tr><td>LocalizeFormatFunction</td><td>object</td><td>N/A</td><td>Callback used to customize the format of the messages that are returned from the Localization Module.</td></tr>
      <tr><td>MaxLength</td><td>int</td><td>N/A</td><td>Sets or gets the maximum number of characters that the user can enter.</td></tr>
      <tr><td>MinLength</td><td>int</td><td>0</td><td>Sets or gets the minimum number of characters that the user can enter.</td></tr>
      <tr><td>Messages</td><td>object</td><td>N/A</td><td>Sets or gets an object specifying strings used in the widget that can be localized. Used in conjunction with the property <strong>locale</strong>. </td></tr>
      <tr><td>Name</td><td>string</td><td>&quot;&quot;</td><td>Sets or gets the name attribute for the element. Name is used when submiting HTML forms.</td></tr>
      <tr><td>Placeholder</td><td>string</td><td>&quot;&quot;</td><td>The placeholder text that is displayed when no value is applied to the element. </td></tr>
      <tr><td>Readonly</td><td>bool</td><td>false</td><td>If enabled the users cannot iteract with the element.</td></tr>
      <tr><td>Required</td><td>bool</td><td>false</td><td>Specifies that the user must fill in a value before submitting a form that contains the element.</td></tr>
      <tr><td>Resizable</td><td>bool</td><td>false</td><td>Enables/ disables the resizing of the element. If enabled a resizing indicator appears in the bottom corner of the input area.</td></tr>
      <tr><td>RightToLeft</td><td>bool</td><td>false</td><td>Sets or gets the value indicating whether the element is aligned to support locales using right-to-left fonts.</td></tr>
      <tr><td>Rows</td><td>int</td><td>5</td><td>The number of visible text lines for the control.</td></tr>
      <tr><td>SelectAllOnFocus</td><td>bool</td><td>false</td><td>Specifies whether the content of the input will be selected on focus.</td></tr>
      <tr><td>SelectionEnd</td><td>int</td><td>0</td><td>Indicates the index of the last character in the current selection.</td></tr>
      <tr><td>SelectionStart</td><td>int</td><td>0</td><td>Indicates the index to the first character in the current selection.</td></tr>
      <tr><td>SpellCheck</td><td>bool</td><td>false</td><td>Specifies whether the element is to have its spelling and grammar checked or not.</td></tr>
      <tr><td>Theme</td><td>string</td><td>&quot;&quot;</td><td>Determines the theme. Theme defines the look of the element</td></tr>
      <tr><td>Unfocusable</td><td>bool</td><td>false</td><td>If is set to true, the element cannot be focused.</td></tr>
      <tr><td>Value</td><td>string</td><td>&quot;&quot;</td><td>Sets or gets the value of the element.</td></tr>
      <tr><td>VerticalScrollBarVisibility</td><td>VerticalScrollBarVisibility</td><td>VerticalScrollBarVisibility.Auto</td><td>Controls vertical scrollbar's visibility. </td></tr>
      <tr><td>Wrap</td><td>MultilineTextBoxWrap</td><td>MultilineTextBoxWrap.Soft</td><td>Indicates how the control wraps text.</td></tr>
  </table>
  <h2 id="MultilineTextBoxMethods"><a id="MultilineTextBox_methods" href="javascript: void(0)" class="anchor"><strong>MultilineTextBox</strong> Methods</a></h2>
  <table class="table">
      <tr><th>Name</th><th>Type</th><th>Arguments</th><th>Description</th></tr>
      <tr><td>Focus</td><td>void</td><td>N/A</td><td>Focuses the element.</td></tr>
      <tr><td>Reset</td><td>void</td><td>N/A</td><td>The method is used to reset the value of the element box to it's initial state.</td></tr>
      <tr><td>Selection</td><td>string</td><td>string displayMode</td><td>Returns the currenctly selected text.</td></tr>
      <tr><td>Select</td><td>void</td><td>object rangeFrom, object rangeTo</td><td>Selects a certain part of the input text. If no arguments are specified the whole text will be selected.</td></tr>
  </table>
  <h2 id="MultilineTextBoxEvents"><a id="MultilineTextBox_events" href="javascript: void(0)" class="anchor"><strong>MultilineTextBox</strong> Events</a></h2>
  <table class="table">
      <tr><th>Name</th><th>Type</th><th>Description</th><th>Event Detail</th></tr>
      <tr><td>OnChange</td><td>EventCallback&lt;Event&gt;</td><td>This event is triggered when the value of the text box is changed.</td><td>dynamic oldValue, dynamic value, dynamic type</td></tr>
      <tr><td>Changed</td><td>event MultilineTextBoxChangedEventHandler</td><td>This event is triggered when the value of the text box is changed.</td><td>MultilineTextBoxChangedEventArgs</td></tr>
    </table>
</div>
